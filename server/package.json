{
  "name": "server",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch server.ts"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "server.js"
}
